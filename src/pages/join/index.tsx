import { Meta } from '@/layouts/Meta';
import { Main } from '@/templates/Main';

const Join = () => (
  <Main
    meta={<Meta title="Join us" description="Join Programmer's University" />}
  >
    <section id="join-section">
      <h1 className="text-black">coming soon...</h1>
    </section>
  </Main>
);

export default Join;
